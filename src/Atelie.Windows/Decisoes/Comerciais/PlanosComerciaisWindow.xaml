<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Atelie.Decisoes.Comerciais"
        xmlns:local1="clr-namespace:Atelie.Decisoes.Comerciais;assembly=Atelie.UI" x:Class="Atelie.Decisoes.Comerciais.PlanosComerciaisWindow"
        mc:Ignorable="d"
        Title="PlanosComerciaisWindow" Height="450" Width="800" Loaded="Window_Loaded">
    <Window.Resources>
        <CollectionViewSource x:Key="planoComercialViewModelViewSource" d:DesignSource="{d:DesignInstance {x:Type local1:PlanoComercialViewModel}, CreateList=True}"/>
        <CollectionViewSource x:Key="planoComercialViewModelItensViewSource" Source="{Binding Itens, Source={StaticResource planoComercialViewModelViewSource}}"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Menu VerticalAlignment="Top">
            <MenuItem Header="Sistema"/>
        </Menu>
        <ToolBar VerticalAlignment="Top" Grid.Row="1">
            <Button Content="Novo Fabricante..."/>
        </ToolBar>
        <Grid Grid.Row="2" DataContext="{StaticResource planoComercialViewModelViewSource}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <DataGrid x:Name="planoComercialViewModelDataGrid" RowDetailsVisibilityMode="VisibleWhenSelected" ItemsSource="{Binding}" EnableRowVirtualization="True" AutoGenerateColumns="False" FontFamily="Calibri" HorizontalGridLinesBrush="#FF8EA9DB" VerticalGridLinesBrush="#FFD4D4D4" FontSize="14" SelectionUnit="Cell">
                <DataGrid.Style>
                    <Style TargetType="{x:Type DataGrid}">
                        <Setter Property="AlternatingRowBackground" Value="#FFD9E1F2" />
                    </Style>
                </DataGrid.Style>
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="{x:Type DataGridColumnHeader}">
                        <Setter Property="FontWeight"  Value="Bold" />
                        <Setter Property="Background" Value="#FF4472C4" />
                        <Setter Property="Foreground" Value="White"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn x:Name="idColumn" IsReadOnly="True" Header="Id" Binding="{Binding Id}"/>
                    <DataGridTextColumn x:Name="nomeColumn" Header="Nome" Binding="{Binding Nome}"/>
                    <DataGridTextColumn x:Name="custoFixoColumn" Header="Custo Fixo" Binding="{Binding CustoFixo}"/>
                    <DataGridTextColumn x:Name="custoFixoPercentualColumn" Header="Custo Fixo Percentual" Binding="{Binding CustoFixoPercentual}"/>
                    <DataGridTextColumn x:Name="custoPercentualColumn" Header="Custo Percentual" Binding="{Binding CustoPercentual}"/>
                    <DataGridTextColumn x:Name="custoVariavelColumn" Header="Custo Variavel" Binding="{Binding CustoVariavel}"/>
                    <DataGridTextColumn x:Name="margemColumn" Header="Margem" Binding="{Binding Margem}"/>
                    <DataGridTextColumn x:Name="margemPercentualColumn" Header="Margem Percentual" Binding="{Binding MargemPercentual}"/>
                    <DataGridTextColumn x:Name="rendaBrutaMensalColumn" Header="Renda Bruta Mensal" Binding="{Binding RendaBrutaMensal}"/>
                    <DataGridTextColumn x:Name="stateColumn" IsReadOnly="True" Header="State" Binding="{Binding State}"/>
                    <DataGridTextColumn x:Name="taxaDeMarcacaoColumn" Header="Taxa De Marcacao" Binding="{Binding TaxaDeMarcacao}"/>
                </DataGrid.Columns>
            </DataGrid>
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />
            <GroupBox Grid.Column="2" Header="Componentes Fabricados" HorizontalAlignment="Left" Height="322" Margin="10,10,0,0" VerticalAlignment="Top" Width="373">
                <DataGrid x:Name="itensDataGrid" AutoGenerateColumns="False" EnableRowVirtualization="True" ItemsSource="{Binding Source={StaticResource planoComercialViewModelItensViewSource}}" Margin="10,10,-2,-12" RowDetailsVisibilityMode="VisibleWhenSelected" FontFamily="Calibri" HorizontalGridLinesBrush="#FF8EA9DB" VerticalGridLinesBrush="#FFD4D4D4" FontSize="14">
                    <DataGrid.Columns>
                        <DataGridTextColumn x:Name="custoDeProducaoValorColumn" Binding="{Binding CustoDeProducaoValor}" Header="Custo De Producao Valor" Width="SizeToHeader"/>
                        <DataGridTextColumn x:Name="modeloCodigoColumn" Binding="{Binding ModeloCodigo}" Header="Modelo Codigo" Width="SizeToHeader"/>
                        <DataGridTextColumn x:Name="modeloNomeColumn" Binding="{Binding ModeloNome}" Header="Modelo Nome" Width="SizeToHeader"/>
                        <DataGridTextColumn x:Name="planoComercialIdColumn" Binding="{Binding PlanoComercialId}" Header="Plano Comercial Id" Width="SizeToHeader"/>
                        <DataGridTextColumn x:Name="precoDeVendaColumn" Binding="{Binding PrecoDeVenda}" Header="Preco De Venda" Width="SizeToHeader"/>
                        <DataGridTextColumn x:Name="stateColumn1" Binding="{Binding State}" Header="State" IsReadOnly="True" Width="SizeToHeader"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>
        <StatusBar Grid.Row="3" VerticalAlignment="Bottom">
            <Label Content="Pronto."/>
        </StatusBar>
    </Grid>
</Window>
